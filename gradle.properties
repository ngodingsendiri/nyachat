# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.caching=true
org.gradle.configuration-cache=true
# Set the maximum number of workers to 4 to avoid overloading the machine.
org.gradle.workers.max=4
# Set the Kotlin compiler execution strategy to in-process to avoid "Could not
# connect to Kotlin compile daemon" error.
kotlin.compiler.execution.strategy=in-process

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true

# L11: versi rilis dari gradle.properties (satu sumber kebenaran, bukan hardcode
# di build.gradle.kts). CI & lokal bisa override via -PappVersion=... -PappVersionCode=...
# r1.6.1 (audit pesan): foto lampiran chat kini di-upload ke Firebase Storage
# & diunduh otomatis penerima; FCM self-skip per-uid; placeholder "Foto";
# kolom Room imageUrl + senderUid (v15). 
# r1.7.1: tanda terima/baca pesan terenkripsi (titik abu/hijau/pelangi + Info
# Pesan), anti-degradasi plaintext saat E2EE belum siap, deploy TTL receipt
# inline (tanpa Cloud Scheduler).
# r1.7.2: bubble foto lebih kecil (ala WA: lebar 280dp, tinggi cap 340dp),
# ripple sentuh mengikuti bentuk bubble, nama aplikasi "NyaChat" (N & C
# kapital) + title login dua warna (Nya hitam / Chat hijau gelap).
# r1.7.3 (permintaan user): footer bubble TEKS pindah ke pojok kiri-bawah
# dengan indikator status lingkaran PENUH di KIRI waktu; bubble GAMBAR tetap
# kanan-bawah seperti sebelumnya.
appVersion=r1.7.3
appVersionCode=39
